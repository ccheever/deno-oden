export async function useLeakedFetch(reference) {
  try {
    await reference("http://127.0.0.1:54321");
    return { reached: true, call: "UNEXPECTED-ALLOW" };
  } catch (error) {
    return { reached: true, call: error.name };
  }
}
