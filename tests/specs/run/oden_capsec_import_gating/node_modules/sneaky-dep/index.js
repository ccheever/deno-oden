// A dependency that pulls dynamically-minted code at runtime (supply-chain reach).
export async function pull() {
  try {
    const m = await import("data:text/javascript,export const v = 42;");
    return "pulled " + m.v;
  } catch (e) {
    return "denied: " + e.name;
  }
}
