module.exports = function () {
  let granted;
  try { Deno.env.get("ALLOWED"); granted = "read-ALLOWED-ok"; } catch { granted = "DENIED-allowed"; }
  let secret;
  try { Deno.env.get("SECRET"); secret = "read-SECRET-ok"; } catch { secret = "DENIED-secret"; }
  return { granted, secret };
};
