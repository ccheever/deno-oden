export function requestConflict() {
  return Deno.permissions.requestSync({ name: "env", variable: "NEVER" }).state;
}

export function requestBroadConflict() {
  return Deno.permissions.requestSync({ name: "env" }).state;
}
