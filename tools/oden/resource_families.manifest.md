# Oden resource-family classification (generated)

Generated by `tools/oden/resource_families.ts`. Every resource in the table
carries an ownership class so a guessed rid cannot cross a package boundary.
`--check` fails if any `impl Resource for` lacks a class here.

## Summary

- **ambient**: 3
- **owner-checked**: 28
- **possession-delegable**: 19
- **terminal**: 2

## ambient

| resource | family | owner-check status | note |
| --- | --- | --- | --- |
| CancelHandle | control:cancel | residual (rid-guess audited, not yet denied) | internal cancel token |
| FetchCancelHandle | fetch:cancel | residual (rid-guess audited, not yet denied) | cancel token; no cross-package authority |
| WasmStreamingResource | wasm:streaming | residual (rid-guess audited, not yet denied) | wasm compile buffer; ephemeral |

## owner-checked

| resource | family | owner-check status | note |
| --- | --- | --- | --- |
| CacheResponseResource | cache:response | residual (rid-guess audited, not yet denied) | cache response body handle |
| ChildResource | process:child | residual (rid-guess audited, not yet denied) | spawn gated at open; child handle owner-checked |
| ChildStderrResource | process:stderr | residual (rid-guess audited, not yet denied) | child stdio fd |
| ChildStdinResource | process:stdin | residual (rid-guess audited, not yet denied) | child stdio fd; ownership follows the child |
| ChildStdoutResource | process:stdout | residual (rid-guess audited, not yet denied) | child stdio fd |
| CipherContext | crypto:cipher | residual (rid-guess audited, not yet denied) | cipher context; opener owns |
| CronResource | cron | residual (rid-guess audited, not yet denied) | cron registration; opener owns |
| DatabaseResource | kv:db | residual (rid-guess audited, not yet denied) | kv handle; opener owns |
| DatabaseWatcherResource | kv:watch | residual (rid-guess audited, not yet denied) | kv watcher follows the db handle |
| DecipherContext | crypto:decipher | residual (rid-guess audited, not yet denied) | decipher context |
| FetchRequestResource | fetch:request | residual (rid-guess audited, not yet denied) | outbound request body |
| FetchResponseResource | fetch:response | residual (rid-guess audited, not yet denied) | response body stream |
| FsEventsResource | fs:watch | residual (rid-guess audited, not yet denied) | Deno.watchFs watcher; opener owns (ENG-23799 gates attach) |
| HeldLockResource | web:lock-held | residual (rid-guess audited, not yet denied) | Web Locks held lock; owner releases |
| HttpClientResource | fetch:client | residual (rid-guess audited, not yet denied) | http client handle |
| HttpConnResource | http:conn | residual (rid-guess audited, not yet denied) | server connection |
| HttpJoinHandle | http:join | residual (rid-guess audited, not yet denied) | serve task join handle |
| HttpRequestBody | http:reqbody | residual (rid-guess audited, not yet denied) | inbound request body |
| HttpStreamReadResource | http:read | residual (rid-guess audited, not yet denied) | server stream read half |
| HttpStreamWriteResource | http:write | residual (rid-guess audited, not yet denied) | server stream write half |
| PendingLockResource | web:lock-pending | residual (rid-guess audited, not yet denied) | Web Locks pending acquire |
| QueueMessageResource | kv:queue-msg | residual (rid-guess audited, not yet denied) | queue message handle |
| RawH1RequestBody | http:reqbody-h1 | residual (rid-guess audited, not yet denied) | h1 inbound body |
| ReadDirResource | fs:readdir | residual (rid-guess audited, not yet denied) | directory read handle; opener owns |
| ServerWebSocket | websocket | residual (rid-guess audited, not yet denied) | endpoint-scoped; opener owns |
| SignalStreamResource | os:signal | residual (rid-guess audited, not yet denied) | signal listener; opener owns |
| UpgradeStream | http:upgrade | residual (rid-guess audited, not yet denied) | upgraded (websocket) stream |
| WsCancelResource | websocket:cancel | residual (rid-guess audited, not yet denied) | cancel handle follows the socket |

## possession-delegable

| resource | family | owner-check status | note |
| --- | --- | --- | --- |
| FileResource | fs:file | residual (rid-guess audited, not yet denied) | open file handle; opener owns, delegable |
| JSDuplexResource | node:duplex | residual (rid-guess audited, not yet denied) | node duplex stream wrapper |
| JSStreamTlsResource | node:tls | residual (rid-guess audited, not yet denied) | node tls stream wrapper |
| MessagePortResource | web:messageport | residual (rid-guess audited, not yet denied) | the structured-transfer channel; possession is authority |
| NamedPipe | net:pipe | residual (rid-guess audited, not yet denied) | windows named pipe |
| NetworkListenerResource | net:listener | residual (rid-guess audited, not yet denied) | server listener socket; delegable to a worker |
| NodeUdpSocketResource | node:udp | residual (rid-guess audited, not yet denied) | node udp socket |
| PipeResource | io:pipe | residual (rid-guess audited, not yet denied) | pipe fd |
| ReadableStreamResource | web:stream | residual (rid-guess audited, not yet denied) | stream body; delegable |
| RecvStreamResource | net:quic-recv | residual (rid-guess audited, not yet denied) | quic recv stream |
| SendStreamResource | net:quic-send | residual (rid-guess audited, not yet denied) | quic send stream |
| TcpStreamResource | net:tcp | residual (rid-guess audited, not yet denied) | socket; endpoint-scoped grant at open |
| ThreadMessageReceiver | worker:message | residual (rid-guess audited, not yet denied) | worker message channel; possession is authority |
| TlsStreamResource | net:tls | residual (rid-guess audited, not yet denied) | tls socket |
| TunnelStreamResource | net:tunnel | residual (rid-guess audited, not yet denied) | tunnel stream |
| UdpSocketResource | net:udp | residual (rid-guess audited, not yet denied) | datagram socket |
| UnixDatagramResource | net:unixgram | residual (rid-guess audited, not yet denied) | unix datagram |
| UnixStreamResource | net:unix | residual (rid-guess audited, not yet denied) | unix socket |
| VsockStreamResource | net:vsock | residual (rid-guess audited, not yet denied) | vsock |

## terminal

| resource | family | owner-check status | note |
| --- | --- | --- | --- |
| DynamicLibraryResource | ffi:dylib | checked | FFI load is compartment-terminating; gated at open, terminal thereafter |
| UnsafeCallbackResource | ffi:callback | checked | FFI callback; terminal |

## Named interim residuals

Families classified owner-checked/possession-delegable whose use-time owner
check is not yet wired: same-process rid guessing for these is audited, not
yet denied (LLP 0001 sequencing — a family is never owner-checked before its
delegation path exists). Wiring the check per family closes each.

- 47 residual families: CacheResponseResource, ChildResource, ChildStderrResource, ChildStdinResource, ChildStdoutResource, CipherContext, CronResource, DatabaseResource, DatabaseWatcherResource, DecipherContext, FetchRequestResource, FetchResponseResource, FileResource, FsEventsResource, HeldLockResource, HttpClientResource, HttpConnResource, HttpJoinHandle, HttpRequestBody, HttpStreamReadResource, HttpStreamWriteResource, JSDuplexResource, JSStreamTlsResource, MessagePortResource, NamedPipe, NetworkListenerResource, NodeUdpSocketResource, PendingLockResource, PipeResource, QueueMessageResource, RawH1RequestBody, ReadDirResource, ReadableStreamResource, RecvStreamResource, SendStreamResource, ServerWebSocket, SignalStreamResource, TcpStreamResource, ThreadMessageReceiver, TlsStreamResource, TunnelStreamResource, UdpSocketResource, UnixDatagramResource, UnixStreamResource, UpgradeStream, VsockStreamResource, WsCancelResource

