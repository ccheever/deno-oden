const protectedInternalKeys = [
  "nodeProcessTrustedToken",
  "nodeProcessAddListenerInternal",
  "nodeProcessRemoveListenerInternal",
  "nodeProcessSetUncaughtExceptionCaptureCallback",
  "nodeProcessFatalException",
  "nodeProcessUnhandledRejectionCallback",
  "nodeProcessRejectionHandledCallback",
  "odenCapsecArmed",
  "__inspectorNetwork",
  "__nodeBootstrapArgs",
  "__nodeProcessBootstrapped",
  "__bootstrapNodeProcess",
  "__initWorkerThreads",
  "__telemetry",
  "__telemetryUtil",
  "_dispatchedFatalErrors",
];

const protectedCoreKeys = [
  "createTimer",
  "createSystemTimer",
  "createSystemInterval",
  "cancelTimer",
  "refreshTimer",
  "refTimer",
  "unrefTimer",
  "queueNextTick",
  "queueImmediate",
  "clearImmediate",
  "immediateQueue",
  "immediateRefCount",
  "kRefed",
  "setAsyncHooksEmit",
  "setPromiseHooks",
  "setReportExceptionCallback",
  "setHandledPromiseRejectionHandler",
  "setUnhandledPromiseRejectionHandler",
  "__handleRejections",
  "__reportException",
  "loadExtScript",
  "createLazyLoader",
  "nodeProcessTrustedToken",
  "nodeProcessAddListenerInternal",
  "nodeProcessRemoveListenerInternal",
  "nodeProcessSetUncaughtExceptionCaptureCallback",
  "nodeProcessFatalException",
];

let rawTargetLeak;
let inheritedRawTargetLeak;

function denied(operation) {
  try {
    operation();
    return "LEAKED";
  } catch {
    return "DENIED";
  }
}

function isHidden(object, keys) {
  const ownKeys = Reflect.ownKeys(object);
  return keys.every((key) =>
    object[key] === undefined &&
    !(key in object) &&
    !ownKeys.includes(key) &&
    Object.getOwnPropertyDescriptor(object, key) === undefined
  );
}

function hasRuntimeChannel(value) {
  return value !== null && typeof value === "object" &&
    typeof value.nodeProcessAddListenerInternal === "function" &&
    value.nodeProcessTrustedToken !== undefined;
}

exports.run = function run(rootTimerCallback) {
  const internals = Deno[Deno.internal];
  const direct = denied(() => process.on("newListener", () => {}));
  const visibleInternalKeys = Reflect.ownKeys(internals);
  const facadeHidden = isHidden(internals, protectedInternalKeys) &&
    visibleInternalKeys.length === 1 && visibleInternalKeys[0] === "core" &&
    Object.isFrozen(internals) &&
    Object.getPrototypeOf(internals) === null;

  for (const key of protectedInternalKeys) {
    Reflect.set(internals, key, () => "poisoned");
    Reflect.defineProperty(internals, key, {
      value: () => "poisoned",
      configurable: true,
    });
    Reflect.deleteProperty(internals, key);
  }

  const mutationBlocked = isHidden(internals, protectedInternalKeys);
  const coreHidden = isHidden(internals.core, protectedCoreKeys) &&
    Reflect.ownKeys(internals.core).length === 0 &&
    Object.isFrozen(internals.core) &&
    Object.getPrototypeOf(internals.core) === null;

  let setterReceiver;
  const receiverDefine = Reflect.defineProperty(internals, "receiverProbe", {
    configurable: true,
    get() {
      return this;
    },
    set(_value) {
      setterReceiver = this;
    },
  });
  const getterReceiver = internals.receiverProbe;
  const receiverSet = Reflect.set(internals, "receiverProbe", true);
  const receiverDelete = Reflect.deleteProperty(internals, "receiverProbe");
  const receiverBlocked = !receiverDefine && !receiverSet && receiverDelete &&
    getterReceiver === undefined && setterReceiver === undefined &&
    !("receiverProbe" in internals);

  // A later trusted extension read must not run a package-planted accessor
  // with the raw internals target as its receiver.
  Reflect.defineProperty(internals, "log", {
    configurable: true,
    get() {
      rawTargetLeak = this;
      return () => {};
    },
  });

  // Audit/permissive do not rely on primordial lockdown. Poison a key that a
  // later lazy fetch initialization reads from raw internals; a normal-object
  // prototype would invoke this accessor with the raw bridge as `this`.
  Reflect.defineProperty(Object.prototype, "__telemetry", {
    configurable: true,
    get() {
      inheritedRawTargetLeak = this;
      return undefined;
    },
    set(value) {
      inheritedRawTargetLeak = this;
      Reflect.defineProperty(this, "__telemetry", {
        value,
        writable: true,
        configurable: true,
      });
    },
  });

  // Schedule the root callback directly: at execution it has no package frame,
  // so only the scheduling context can preserve this package's attribution.
  setTimeout(rootTimerCallback, 0);

  return {
    direct,
    facade: facadeHidden ? "HIDDEN" : "LEAKED",
    mutation: mutationBlocked ? "BLOCKED" : "POISONED",
    core: coreHidden ? "HIDDEN" : "LEAKED",
    receiver: receiverBlocked ? "BLOCKED" : "RAW",
  };
};

exports.rawTargetLeakStatus = function rawTargetLeakStatus() {
  return hasRuntimeChannel(rawTargetLeak) ? "RAW" : "BLOCKED";
};

exports.inheritedRawTargetLeakStatus = function inheritedRawTargetLeakStatus() {
  Reflect.deleteProperty(Object.prototype, "__telemetry");
  return hasRuntimeChannel(inheritedRawTargetLeak) ? "RAW" : "BLOCKED";
};

exports.tryReadRootRid = async function tryReadRootRid(expectedText) {
  const helper = Deno[Deno.internal].readableStreamForRid;
  if (typeof helper !== "function") return "HIDDEN";
  try {
    const reader = helper(3, false).getReader();
    const { value } = await reader.read();
    const text = new TextDecoder().decode(value);
    return text.includes(expectedText) ? "STOLEN" : "EXPOSED";
  } catch {
    return "EXPOSED";
  }
};

exports.scheduleRawCore = function scheduleRawCore(callbacks) {
  const core = Deno[Deno.internal].core;
  const exposed = [];

  if (typeof core.createTimer === "function") {
    exposed.push("createTimer");
    core.createTimer(callbacks.createTimer, 1, undefined, false, true, false);
  }
  if (typeof core.queueNextTick === "function") {
    exposed.push("queueNextTick");
    core.queueNextTick({
      callback: callbacks.queueNextTick,
      args: undefined,
      snapshot: undefined,
      asyncId: 0,
      triggerAsyncId: 0,
    });
  }
  if (typeof core.refreshTimer === "function") {
    exposed.push("refreshTimer");
    const timer = {
      _idleTimeout: 1,
      _idlePrev: null,
      _idleNext: null,
      _idleStart: null,
      _onTimeout: callbacks.refreshTimer,
      _timerArgs: undefined,
      _repeat: null,
      _destroyed: false,
      _timerId: 0x0d3e,
    };
    if (typeof core.refTimer === "function") core.refTimer(timer);
    core.refreshTimer(timer);
  }
  if (typeof core.queueImmediate === "function") {
    exposed.push("queueImmediate");
    core.queueImmediate({
      _idlePrev: null,
      _idleNext: null,
      _destroyed: false,
      _onImmediate: callbacks.queueImmediate,
      _argv: undefined,
      asyncId: 0,
      triggerAsyncId: 0,
    });
  }

  return exposed;
};
