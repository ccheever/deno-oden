exports.probe = function () {
  let websocket;
  try {
    websocket = typeof WebSocket;
  } catch (error) {
    websocket = error.name;
  }
  return {
    fetch: typeof fetch,
    eventSource: typeof EventSource,
    websocket,
  };
};
