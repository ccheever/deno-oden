import { read, schedule as libSchedule } from "../lib-dep/index.js";

export function schedule() {
  // Binding supplies the label without leaving an evil-dep wrapper frame live
  // when the callback executes.
  setTimeout(read.bind(null, "async-deputy"), 0);
}

export function scheduleThroughDeputy() {
  libSchedule();
}
