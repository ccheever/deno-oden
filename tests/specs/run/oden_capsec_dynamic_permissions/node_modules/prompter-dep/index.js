const descriptor = { name: "env", variable: "PROMPT_ME" };

export function exercise() {
  return {
    first: Deno.permissions.requestSync(descriptor).state,
    second: Deno.permissions.requestSync(descriptor).state,
  };
}
