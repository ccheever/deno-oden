export function reachability() {
  return {
    fetch: typeof fetch,
    eventSource: typeof EventSource,
    websocket: (() => {
      try {
        return typeof WebSocket;
      } catch (error) {
        return error.name;
      }
    })(),
  };
}
