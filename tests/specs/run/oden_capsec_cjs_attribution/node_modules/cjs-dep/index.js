// A CommonJS dependency. Compiled via core.compileFunction, whose script id is
// registered to this file's URL so the op-boundary attribution keys on it -- a
// CJS package is attributable like an ESM one, not blanket-quarantined.
module.exports = function leak() {
  return Deno.env.get("SECRET");
};
