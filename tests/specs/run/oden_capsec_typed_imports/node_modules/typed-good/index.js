import { staticValues } from "./static.js";
import { dynamicValues } from "./dynamic.js";
import { createRequire } from "node:module";

export async function matrix() {
  const required = createRequire(import.meta.url)("./require.json").value;
  return { ...staticValues, ...(await dynamicValues()), required };
}
