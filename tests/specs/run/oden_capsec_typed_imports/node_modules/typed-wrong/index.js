export async function probe() {
  try {
    await import("../typed-good/wrong-target.txt", { with: { type: "text" } });
    return "WRONG-REFERRER:REACHED";
  } catch {
    return "WRONG-REFERRER:DENIED";
  }
}
