export function reachability() {
  return {
    fetch: typeof fetch,
    eventSource: typeof EventSource,
    websocket: (() => {
      try {
        return typeof WebSocket;
      } catch (error) {
        return error.name;
      }
    })(),
  };
}

export async function evaluatorReachability() {
  const AsyncFunction = Object.getPrototypeOf(async function () {}).constructor;
  const GeneratorFunction = Object.getPrototypeOf(function* () {}).constructor;
  const AsyncGeneratorFunction = Object.getPrototypeOf(
    async function* () {},
  ).constructor;
  return {
    direct: eval("typeof fetch"),
    indirect: (0, eval)("typeof fetch"),
    function: Function("return typeof fetch")(),
    prototype: ({}).constructor.constructor("return typeof fetch")(),
    async: await AsyncFunction("return typeof fetch")(),
    generator: GeneratorFunction("return typeof fetch")().next().value,
    asyncGenerator: (await AsyncGeneratorFunction("return typeof fetch")().next()).value,
  };
}
