export function direct() {
  return fetch;
}

export function globalPaths() {
  return [
    () => globalThis.fetch,
    () => globalThis["fet" + "ch"],
    () => self.fetch,
  ];
}

export function enumeration() {
  return {
    has: "fetch" in globalThis,
    own: Object.getOwnPropertyNames(globalThis).includes("fetch"),
  };
}

export function reflective() {
  return Reflect.get(globalThis, "fetch");
}

export function evalPaths() {
  return [
    () => eval("typeof fetch"),
    () => (0, eval)("typeof fetch"),
  ];
}

async function callRecovered(reference) {
  try {
    await reference("http://127.0.0.1:54321");
    return "UNEXPECTED-ALLOW";
  } catch (error) {
    return error.name;
  }
}

export async function functionResidual() {
  const reference = Function("return fetch")();
  return { reached: typeof reference, call: await callRecovered(reference) };
}

export async function prototypeResidual() {
  const reference = ({}).constructor.constructor("return fetch")();
  return { reached: typeof reference, call: await callRecovered(reference) };
}

export function strictThis() {
  return (function () {
    return this;
  })() === undefined;
}

export function namespaces() {
  return [() => Deno, () => process, () => Worker];
}

export function helperPath() {
  return __oden_compartment_globals__().fetch;
}

export function asyncReach() {
  return new Promise((resolve) => {
    setTimeout(() => {
      try {
        resolve(`UNEXPECTED:${typeof fetch}`);
      } catch (error) {
        resolve(error.name);
      }
    }, 0);
  });
}

export function localBinding(fetch) {
  return fetch;
}
